# Quote Generator

A minimal, zero-dependency Python command-line utility that prints a beautifully formatted random inspirational quote each time it is executed.

## Setup Instructions

### 1. Create a Virtual Environment
```bash
python -m venv .venv